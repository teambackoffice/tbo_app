import 'package:flutter/material.dart';

class CRMAllLeads extends StatelessWidget {
  const CRMAllLeads({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('CRM All Leads')),
      body: Center(child: Text('This is the CRM All Leads screen')),
    );
  }
}
